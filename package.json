{
  "name": "elysia-header-leak-poc",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "PoC server project: request headers leaked via ValidationError.found on a headers-validated route",
  "scripts": {
    "victim": "bun run victim.ts",
    "proxy": "bun run proxy.ts",
    "exploit": "bun run exploit.ts"
  },
  "dependencies": {
    "elysia": "1.4.29"
  }
}
